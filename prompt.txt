Set-Content -Path prompt.txt -Encoding UTF8 -Value "Je veux ajouter un bouton de dictée vocale directement dans le formulaire (formulaire.html), sur le champ notes brutes (le grand champ de saisie principal). Technologie : Web Speech API (SpeechRecognition), native au navigateur, zéro dépendance externe. Comportement attendu : Un bouton micro apparaît à côté du champ notes brutes. Clic sur le bouton → la reconnaissance vocale démarre, le bouton change d'apparence (rouge pulsant ou icône active) pour signaler que l'enregistrement est en cours. La transcription s'insère en temps réel dans le champ (pas en remplacement — en ajout à ce qui est déjà écrit, avec un espace avant si le champ n'est pas vide). Clic à nouveau → arrêt de l'enregistrement. La langue de reconnaissance est le français fr-FR. Si le navigateur ne supporte pas la Web Speech API Firefox Safari : le bouton n'apparaît pas du tout, pas de message d'erreur, juste absent. Style : cohérent avec le reste du formulaire couleurs border-radius ton violet rose de Florita. Le bouton doit être clairement identifiable comme micro sans être intrusif. Montre-moi le code avant de l'appliquer."